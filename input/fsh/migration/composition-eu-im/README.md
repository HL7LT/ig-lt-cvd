# Composition 1: Based on EU IM (CompositionLt / CompositionEu)

One composition conforming to the EU-based hierarchy, valid against existing LT/EU profiles. Does not replace any existing resource.

## Instance

- **composition-cvd-eu-im-example.fsh** – Composition instance with profile CompositionLt (parent CompositionEu). Document type LOINC 83539-7 (ŠKL rizikos vertinimas). Sections use LOINC section codes and reference existing CVD IG examples (e.g. CarePlan) where applicable.

## Usage

Review the instance; if suitable, it can be moved to the main IG examples (e.g. under `screening/` or a new `examples/` folder) and referenced from the IG. Encounter reference is optional and can be added when an Encounter example exists.
